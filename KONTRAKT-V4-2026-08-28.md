# Kontrakt — Aura Zľavy V4: dashboard, KPI produktov, hromadné zľavy (28. 8. 2026)

Vetva: `feat/v4-dashboard` (z `feat/bez-loginu`) · Zadal: Samuel ·
Rozsah odsúhlasený interaktívne 28. 8. 2026 (8 kľúčových otázok) +
`docs/62-DOTAZNIK-UX-V4-60-OTAZOK.md` (zvyšok podľa návrhov, predvolené).

## 1. Cieľ

Appka má odpovedať na slučku „čo leží → zľavni → pomohlo to?" z jednej
obrazovky: prvá strana ukáže predaje a top/flop produkty, Produkty dostanú
individuálne KPI, Zľavy hromadné nastavovanie cez presety a predvýber.
Všade, kde je dnes `product_id`, bude `referencia · názov`. Dáta z API sa
kombinujú lokálne (sync → MariaDB → JOIN), nikdy pri renderi.

## 2. Rozhodnutia (odsúhlasené 28. 8. 2026)

| # | Rozhodnutie | Dôvod |
|---|---|---|
| **D109** | Jeden veľký sprint (L), nie dva. | Voľba Samuela; ponúknuté bolo rozdelenie V4a/V4b. Riziko prerobenia obrazoviek nesie sprint. |
| **D110** | Tržby v € sa ukladajú a zobrazujú, AK objednávkové API vracia ceny položiek; overí sa sondou v prvej vlne. Ak nie, ostávajú kusy a appka to prizná (I11). | Dopad zliav sa bez € nedá povedať poctivo. |
| **D111** | Backfill objednávok 180 dní, po častiach, s rešpektom k dennému čítaciemu rozpočtu (katalóg má prednosť). | Pokrýva okná obrátkovosti aj uplift; 365 d je dvojnásobný rozpočet bez čítajúcej obrazovky. |
| **D112** | Hromadné zľavy = pomenované presety (filter + pásma + trvanie, spustenie na klik) + checkboxový predvýber v Produktoch → „pridať do zľavy". Dry-run + potvrdenie VŽDY nanovo (I3). Viac kampaní jedným krokom sa NEROBÍ. | N zápisov z jedného úmyslu zvyšuje riziko; preset šetrí čas bez oslabenia brán. |
| **D113** | Dashboard = prepracovaný Prehľad (jedna prvá strana). Predaje + top/flop hore, bežiace zľavy pod nimi, stav appky zúžený do pásu s rozklikom. | Dve „prvé strany" si konkurujú; Prehľad predajovú sekciu už má. |
| **D114** | KPI riadku produktu: predané ks 30 d, ks 90 d, obrátkovosť, posledný predaj, cena, aktívna zľava %, marža (podmienená D110/sondou nákupnej ceny). | Plná sada podľa voľby Samuela; všetko z lokálnej DB. |
| **D115** | Detail produktu = rozšírený bočný panel (denná krivka 90 d s oknami zliav + uplift). Žiadna nová stránka. | Panel existuje; pozor na pascu d00e081 — definícia porovnávacích okien bude explicitná. |
| **D116** | Referencia všade, kde je `product_id` (Produkty, detail, výber zľavy, položky kampane, audit) vo formáte „ref · názov"; id v technickom detaile. Katalóg dostane stĺpec `reference` aditívnou migráciou; audit sa dopĺňa JOIN-om, história sa NEPREPISUJE (I4). Chýbajúca referencia = pomlčka + id (I11). | Id je pre appku, referencia pre človeka. |

**Predvolené podľa dotazníka** (dá sa zmeniť aj počas behu, sú reverzibilné):
okno predajov prepínač 7/30/90 (default 30) · top/flop 10 riadkov · hlavný graf
= denná krivka s podfarbenými oknami zliav a priznanými medzerami · manuálny
refresh · desktop-first · zoradenie ležiaky prvé · hľadanie podľa ref/názvu +
filter „má aktívnu zľavu" · bez CSV exportu · KPI na úrovni produktu ·
stránkovanie 100 · značka „bez predaja" len nad stiahnutými dňami · pásma
zostávajú · prefill „zopakovať zľavu" · trvanie 14 d · kolízie sa vylučujú
s hláškou · výber nad strop sa oreže (ScopeRelease) · timeline zliav na stránke
Zľavy · sync 1× denne v noci + manuálne tlačidlo · denné agregáty per produkt ·
KPI v SQL repozitárov · bez materializácie (merať) · KISS dizajn ·
UI text „Prehľad", nie „Dashboard".

## 2b. Revízia po sonde API (28. 8. 2026)

Sonda bežala s kľúčom, ktorý Samuel výslovne povolil použiť. **Vyvrátila tri
rozhodnutia §2** a jedno potvrdila lepšie, než čakal kontrakt. Kľúč bol po sonde
zmazaný z disku a do repa sa nedostal (I1); je v transkripte v plnom texte,
takže ho treba **rotovať**.

### Zmerané fakty (nie domnienky)

| Fakt | Dôkaz |
|---|---|
| Kľúč má **všetky štyri scopes** — `orders:read`, `order:stats`, `product:read`, `product:edit` — bez expirácie. Kvóta **20/min, ~200/deň**. | `GET /api/whoami` → `scopes: [order:stats, orders:read, product:edit, product:read]`, `remaining: {per_minute: 19, per_day: 199}` |
| **IP je zabanovaná.** Po prvom úspešnom `whoami` vracia VŠETKO `{"error":"ip_banned"}` — `getFull`, objednávky, aj **verejné** čítanie katalógu bez kľúča. | `GET /api/products?per_page=2` (bez kľúča) → `ip_banned`; `docs/60` to isté meralo 24. 8. |
| `getFull` vracia `reference`, `purchase_price`, `margin`, `margin_percent`, `sell_price`, `sell_price_with_vat`, `last_time_in_order`, `qty`, `qty_in_orders`, `supplier`, `reduction_percent/from/to`, `categories`, `active`, `date_add`, `ean13`. | `docs/api/sperky-api-v5.md` §getFull (riadky 296–314) |
| **Ceny položiek objednávky API NEVRACIA.** `order/get` → `products: [{id, qty}]`. `order` list → len `id, date_add, total_paid, currency`. | `docs/api/sperky-api-v5.md` riadky 78–134 |
| **Batch NEZNIŽUJE kvótu.** „Batching does not reduce your rate-limit cost — 25 items still spend 25 hits (plus 1 for the batch call itself)." Opted-in sú len `products/get` a `order/get` — `getFull` medzi nimi NIE JE. | `docs/api/sperky-api-v4.md` §Batch (riadky 389–411) |
| Aritmetika: 41 348 produktov × `getFull` pri ~200/deň = **~207 dní**. Per-produkt predaje za okno = 1 × `order/get` na KAŽDÚ objednávku. | výpočet z dvoch riadkov vyššie |

`docs/61-ZIADOST-PRODUCT-READ-2026-08-25.md` je pre TENTO kľúč **bezpredmetná** —
`product:read` má. Platná zostáva `docs/60` (ban).

### Revidované rozhodnutia

| # | Rozhodnutie | Ruší / mení |
|---|---|---|
| **D117** | Tržba v € **len na úrovni eshopu** (denná suma z `total_paid`), per produkt **výhradne kusy**. Marža per produkt ide z `getFull` (`margin`, `margin_percent`, `purchase_price`). Rozdeľovať `total_paid` medzi položky sa NESMIE. | **mení D110.** Ceny položiek API nedáva; rozdelenie by bolo vymyslené číslo (poštovné, zľavy, kupóny) → I11. |
| **D118** | Obohacovanie `getFull` je **prioritizované + na dopyt**: (1) produkt, ktorý používateľ otvorí, sa dotiahne HNEĎ; (2) na pozadí dávka ~150/deň v poradí priority — povolený zoznam, produkty v kampaniach, potom ostatné; (3) neobohatený produkt má „nevieme" (pomlčka), NIKDY nulu ani odhad. Zvyšok kvóty (~50) zostáva na canary, sondy a ad-hoc čítania. | **mení K1.** Celý katalóg sa obohatiť nedá (~207 dní). |
| **D119** | Obrátkovosť a značka „bez predaja" stoja na `qty_in_orders` a `last_time_in_order` z `getFull` — jeden request na produkt namiesto tisícov. Okno 30/90 dní sa ukazuje **len pre dni, ktoré sú naozaj stiahnuté**, s priznanou medzerou. | **ruší D111** (180-dňový backfill objednávok). Backfill sa neruší ako kód, ale nespúšťa sa ako plošná úloha. |
| **D120** | Sprint sa stavia **offline proti mock shopu** (I6 — tak sa v tomto repe testuje vždy). Obohacovanie je za bránou dosiahnuteľnosti API: keď shop vracia `ip_banned`, appka to POVIE a nemení dáta. Odblokovanie IP je akcia Samuela (`docs/60` → delaja@fedorco.sk). | dopĺňa §4 R2. |
| **D121** | **Produkt s NEZNÁMYMI predajmi sa do pásiem zľavy nezaradí vôbec.** Dry-run ho vypíše oddelene ako „nevieme, preskočené" s počtom. Pásmo sa NESMIE počítať z `unitsSold`, kde „nevieme" prišlo ako nula. | Zelená brána našla, že `soldBucketOf(0)` zaradí neznámy produkt do pásma `none` s `DEFAULT_TIER_PERCENT` **30 %** — teda najhlbšou zľavou. Kým je IP zabanovaná a obohacovanie stojí, väčšina katalógu vyzerá ako ležiak, takže by 30 % dostali tisíce produktov na základe čísla, ktoré appka nezmerala. Fail-closed: appka nezapíše zľavu z nemeraného predpokladu (I11). Voľba Samuela 28. 8. 2026 z troch variantov. |

### Čo to znamená pre KPI (D114)

Z `getFull` (1 request/produkt): referencia · cena · **marža € aj %** · aktívna
zľava % · sklad · celkovo predané · posledný predaj · dodávateľ · kategórie.
Z lokálnych predajov (kde sú stiahnuté): ks za okno 30/90 d s priznanou medzerou.
**Žiadne KPI sa nedopočítava z ničoho** — čo nie je obohatené, je pomlčka.

### Doplnenie po prekliku 31. 8. 2026 — D121 najprv NEPLATILO END-TO-END (zavreté v ten istý deň)

Preklik v prehliadači nad nasadeným buildom (`9f7f207`) našel to, čo 3756
testov nezachytilo, a je to presne dôvod, prečo kritérium K11 v tomto kontrakte
je.

**Obrazovka Nová zľava hlási „10 000 produktov dostane zľavu · 30 %"**, hoci
predaje appka nepozná. D121 je zapojené len na klientskej strane: model
`SelectableRow.unitsSold` „nevieme" vyjadriť DOKÁŽE (`number | null`), ale
server ho nikdy nepošle —

- `src/app/api/catalog/search/route.ts:678` má `unitsSold: fact?.unitsSold ?? 0`,
- `catalogRepo.search()` má `COALESCE(s.units, 0)` bez brány `status = 'complete'`,
- okno je 180 dní a stiahnuté sú **2**, takže KAŽDÝ produkt príde ako `0`.

Overené priamo z appky: `GET /api/catalog/search?soldWindowDays=180` vracia
`"unitsSold": 0` ako číslo, nie `null`. `soldBucketOf(0)` je preto legitímne
vedro `none` s 30 % — model sa zachoval správne, len dostal nepravdivý vstup.
Rovnaká príčina robí z filtra „0 predaných" filter „nevieme".

**Commit `9f7f207` teda tvrdí o D121 viac, než platí.** Model a zápisová cesta
(`discountWriteRequest()`) sú mutačne overené a pripravené; chýba serverová
polovica. Tú rieši samostatná úloha „Add `status='complete'` gate to
`catalogRepo` unitsSold", ktorá beží v inej session — preto sa jej tu vedome
NEDOTÝKAM (dva zápisy do tých istých súborov naraz sú v tomto repe zapísaná
pasca). D121 sa smie vykázať ako splnené až vtedy, keď `/api/catalog/search`
pošle `null` a obrazovka Nová zľava ukáže preskočené produkty počtom.

#### Čím sa to zavrelo (31. 8. 2026, uzatvárací beh zelenej brány)

Zistenie vyššie platí presne tak, ako je napísané — a je zavreté. Serverová
polovica prišla z tej druhej session a zelená brána ju dokázala na BEŽIACEJ
appke, nie v pracovnej kópii:

- `curl "http://localhost:3070/api/catalog/search?soldWindowDays=180&perPage=1"`
  po rebuilde vracia `unitsSold: null` (predtým `0`), `counts.soldUnknown =
  40 511`, vedrá `sold = {none: 0, low: 805, mid: 30, high: 2}`, `total =
  41 348` — teda vedrá + `soldUnknown` = `total`. Podmienka „`/api/catalog/search`
  pošle `null`" je splnená end-to-end.
- Obrazovka Nová zľava už neklame: brána ručne vpísaného počtu žiada číslo,
  ktoré sa naozaj ZAPÍŠE (`discountedCount = tieredProductIds.length`),
  oddelene od veľkosti VÝBERU, a pri výbere, ktorý celý padne do „nevieme",
  povie dôvod menom („Ani jeden z vybraných produktov nemá predaj za toto okno
  zmeraný (N)…"). Predtým brána potvrdzovala 200 a zapisovalo sa 5.
- Prázdny výber už netvrdí „filtru nevyhovuje ani jeden produkt" — nová
  `emptySelectionText()` rozlišuje „nič nevyhovuje" od „predaje nepoznáme"
  (dokázané preklikom na `/zlavy/nova`).

**Mutačné overenie zelenej brány (a samostatný nález).** Vrátenie `?? 0` na obe
miesta v `src/app/api/catalog/search/route.ts` (r. 433, 738) najprv **NEZČERVENALO
NIČ** — route nemala ani jeden test; `predaje-brana-pokrytia.spec.ts` meria
repozitár, nie prepis na odpoveď. Po dopísaní sekcie 1b v
`test/integration/routes-v8.spec.ts` (route-level test `null/4/0` + tvarová
závora proti `?? 0` nad zdrojom route) tá istá mutácia dala
`2 failed | 9 passed`. Mutácia bola odstránená a stav overený grepom
(`433: unitsSold: row.unitsSold`, `738: unitsSold: fact === undefined ? null :
fact.unitsSold`), opakovaný beh 11/11. **D121 sa teda smie vykázať ako
splnené.**

Zároveň sa ukázalo, že veta „KPI ukazujú `≥ 0`" v odstavci nižšie opisovala
CHYBU, nie správne chovanie: `kpiUnitsCell()` kreslilo `≥ 0` na ~40 511 riadkoch
(server posiela `{ value: 0, gap: 'days_missing' }`). Zavreté v
`src/components/products/sold-coverage.ts` dvomi nezávislými závorami; preklik po
rebuilde dal na `/produkty` `{"vyskytov_ge0":0,"vyskytov_ge":0,"pomlciek":1003}`.

**Stav K11:** preklik prebehol, screenshoty vznikli pre päť obrazoviek nad
zlomom (Prehľad, Produkty, Nová zľava, Zľavy, Nastavenia → Čo smie robiť).
Zrolovaný obsah sa nedal odfotiť — panel prehliadača po scrolle prestane
kompozitovať (overené, že nejde o chybu layoutu: `scrollY 800`, `docH 2005`,
63 sekcií v DOM). Čo preklik potvrdil ako správne: appka priznáva ban
(„Shop odmieta našu IP adresu", „Eshop odmieta čítanie z tejto IP adresy"),
KPI ukazujú `≥ 0` a pomlčky namiesto vymyslených núl, referencia je „kód a EAN
— zamknuté", nadpis presetov hovorí „PREDPLNIA FORMULÁR NOVEJ ZĽAVY", rozsah
hlási „uvoľnenie si vypýta potvrdenie" (D106) a rezerva zápisov drží
(čítania 53/240, zápisy zostávajú 200/200).

## 3. Rozsah NIE (vedome mimo)

Mobil/tablet · e-mail/push notifikácie · KPI variantov · automatické delenie
výberu na viac kampaní · CSV export · nový dizajn systém · druhý používateľ ·
sezónne porovnania nad 180 d.

## 4. Predpoklady a riziká (povedané nahlas)

- **P0 — kľúč.** Kľúč poslaný v chate 28. 8. je UNIKNUTÝ; Samuel vygeneruje
  nový (scopes: čítanie objednávok + produktov) a vloží ho cez Nastavenia →
  Kľúče. Bez neho sa predajová časť nedá stavať nad skutočnými dátami —
  sonda a backfill na ňom stoja. Čítanie katalógu (referencia, nákupná cena)
  kľúč nepotrebuje (verejné čítania).
- **R1 — sonda môže vyvrátiť D110/D114-maržu.** Ak API nedáva ceny položiek /
  nákupnú cenu, € stĺpce a marža sa vynechajú a appka medzeru prizná. Kontrakt
  sa tým NEmení, len sa zúži výstup (zapíše sa do §7).
- **R2 — `forbidden` na objednávkach.** Ak sonda s novým kľúčom znova vráti
  `forbidden`, ide žiadosť IT (vzor `docs/60/61`), beh sa v predajovej časti
  zastaví a dokončí sa katalógová + UI časť nad tým, čo je.
- **R3 — backfill trvá dni.** 180 d sa sťahuje po častiach v rozpočte;
  sprint dodá a spustí runner, plné dáta nabehnú priebežne. Obrazovky medzery
  priznávajú, takže sa dajú prevziať hneď.
- **R4 — jeden veľký sprint (D109).** Obrazovky sa stavajú súbežne s dátami;
  ak sonda zmení schému, UI vlna sa posunie, nie prerobí (rozhrania najprv).

## 5. Akceptačné kritériá

| # | Kritérium | Dôkaz |
|---|---|---|
| K1 | `catalog_cache` má `reference` (+ nákupnú cenu, ak sonda potvrdí) a sync ich plní; migrácia aditívna, žiadna úprava starých migrácií | test nad DB + `git diff db/migrations` len nové súbory |
| K2 | Denné agregáty predajov (ks + € ak D110 platí) za 180 d sa sťahujú po častiach bez prekročenia denného rozpočtu; medzery sú v DB rozlíšiteľné od núl | integračný test + audit rozpočtu |
| K3 | Prehľad: prepínač okna, top/flop 10, graf s oknami zliav a priznanými medzerami, karta bežiacich zliav, stavový pás | preklik + screenshot |
| K4 | Produkty: KPI stĺpce (D114), hľadanie ref/názov, filter „má aktívnu zľavu", ležiaky prvé, checkboxový predvýber | preklik + testy |
| K5 | Detail panel: krivka 90 d s oknami zliav produktu + uplift s explicitne definovanými oknami | test definície okien + preklik |
| K6 | „ref · názov" všade kde bol product_id; audit JOIN-om, bez prepisu histórie; chýbajúca ref = pomlčka | grep + testy + preklik |
| K7 | Presety: uložiť/spustiť/zmazať; spustenie VŽDY prejde dry-runom + potvrdením — `no-write-without-confirm` zelený a mutačne overený | mutačný test |
| K8 | Obrazovky nečítajú shop API pri renderi — všetko z lokálnej DB | grep volaní v render ceste + review |
| K9 | Celý balík zelený (typecheck, lint, `npm test`), žiadny nový `.skip`; beh v izolácii | výstup v reporte |
| K10 | DB len aditívne migrácie; pred migráciou mysqldump do `backups/` (držať posledné 3) | záloha existuje + `git diff` |
| K11 | Preklik v prehliadači so screenshotmi za účasti Samuela (panel musí byť zobrazený) | screenshoty v reporte |

## 6. Plán vĺn (agentová orchestrácia)

0. **Príprava** (main loop): dokončiť D108 test + commit na `feat/bez-loginu`;
   založiť `feat/v4-dashboard`; mysqldump záloha.
1. **Sonda + schéma** (2–3 agenti): sonda `getFull` (reference, nákupná cena)
   a objednávok (ceny položiek — čaká na P0 kľúč); migrácia 0014+; rozšírenie
   katalógového syncu; backfill runner s rozpočtom. Testy nad DB.
2. **Čítacia vrstva** (2–3 agenti): KPI SQL v repozitároch, rozšírenie
   `insights` endpointov (product, sales-daily, timeline), kontrakty typov.
   Testy vrátane medzier v dátach.
3. **Obrazovky** (4 agenti, disjunktné sady): Prehľad · Produkty (KPI +
   hľadanie + predvýber) · detail panel (graf + uplift) · Zľavy (presety +
   timeline). UI texty slovensky.
4. **Zelená brána + review** (2 agenti, effort high): celý balík v izolácii;
   review celého diffu proti kontraktu vrátane security prehliadky (presety sú
   nová mutácia lokálnej DB — dedia origin check D72 a potvrdenia I3; žiadna
   nová zápisová cesta do shopu nesmie vzniknúť).
5. **Overenie + report**: preklik so screenshotmi (K11, so Samuelom), §7
   Výsledok, aktualizácia CLAUDE.md/README, handoff.

## 7. Výsledok (31. 8. 2026, po uzatváracom behu zelenej brány)

### Stav brány kvality

`npx tsc --noEmit` — 0 chýb, prázdny výstup, exit 0 (overené naposledy po
VŠETKÝCH zmenách) · `npm run lint` (eslint .) — 0 problémov · `npm test`
**200/200 súborov, 3888/3888 testov** zelených, 0 failed, 0 skipped, 135,61 s,
jeden celý beh · `npm run check-compose-bind` OK („jediný publikovaný port je
`127.0.0.1:3070:3070` na `ovl-zliav-caddy`", I5).

Vstupné číslo tohto behu bolo 193/3770. Rozdiel 7 súborov sú necommitnuté spec
súbory staviteľov, ktoré do tej štatistiky ešte nešli
(`test/integration/allowlist-strop-poziadaviek.spec.ts`,
`predaje-brana-pokrytia.spec.ts`, `trzba-prazdny-den.spec.ts`,
`test/unit/okna-rebricka.spec.ts`, `panel-fakty-dvakrat.spec.ts`,
`rozpocet-rezerva-ui.spec.ts`, `zlava-detail-rozpad-polozek.spec.ts`);
193 + 7 = 200. Z prírastku testov je 15 z uzatváracieho behu
(`produkty-kpi-bunky` +2, `predajnost-pokrytie-vyber` +8,
`nova-zlava-potvrdenie` +2, `produkty-v10` +1, `routes-v8` +2).

**Žiadny nový `.skip`/`.todo`/`.only`** a žiadny odstránený.
`no-write-without-confirm.spec.ts`, `origin-check-po-loginu.spec.ts`,
`no-orders-scope.spec.ts` a `preset-nie-je-zapisova-cesta.spec.ts` sa
nedotkol nikto (nie sú v `git status` ako zmenené).

**Izolácia:** pred plným behom aj pred každým čiastkovým behom overené
`ps -W | grep -i -c vitest` → **0**. Signatúra `SqlError 1062 Duplicate entry
'1' for key 'PRIMARY' — INSERT INTO settings (id) VALUES (1)` sa vo výstupe
nevyskytla ani raz, takže sa nemuselo nič rozlišovať ani opakovať. Jediné
`level:"error"` riadky v logu sú zámerné testovacie sabotáže
(`shop_key_rejected`, `confirmation_mismatch`, `setStatus sabotovaný testom`,
`tržba explodovala`).

`npm run e2e` a `npm run test:build` **NEBEŽALI** — neboli súčasťou zadanej
zelenej brány. Predchádzajúca poznámka platí ďalej:
`test/e2e/produkty-detail-filtre.spec.ts:101` očakáva text „predaných za
posledných 360 dní"; po oprave dominanty tam je „koľko sa predalo za posledných
360 dní", takže pri prvom spustení e2e treba tvrdenie upraviť.

**Nasadenie.** `scripts/backup.sh` →
`backups/ovl_zliav-20260831-165620.sql.gz` (1,5 MB), potom
`docker compose build ovl-zliav-app` + `docker compose up -d` (dvakrát, druhý raz
nad finálnym stromom), `docker inspect` → `healthy`. Tým sa aplikovala migrácia
`0016_stav_citania_trzby.sql`: pred rebuildom končilo
`SELECT id,name FROM ovl_zliav._migrations ORDER BY id DESC` na
`15 0015_presety_zliav.sql` a tabuľka `shop_revenue_read_state` v
`information_schema` NEBOLA (COUNT = 0); po rebuilde je tam `16` a COUNT = **1**.
Migrácie sa needitovali (`0014`, `0015` sú v `git status` nedotknuté).
`curl /api/insights/revenue-daily?days=30` → **HTTP 200** s poľom `dayStates`
(predtým by to bolo 500). V logu appky za posledných 5 minút nie je ani jeden
`ER_NO_SUCH_TABLE`; jediné chyby sú známy `ip_banned` a
`sales_sync_blocked/forbidden`.

### Akceptačné kritériá

| # | Verdikt | Dôkaz |
|---|---|---|
| K1 | **splnené v revidovanom tvare (D118)** | Migrácia `0014` aplikovaná a checksum-uzamknutá, aditívna (`catalog_cache` + `reference`, `purchase_price`, `margin(_percent)`, `sell_price_with_vat`, `last_time_in_order`, `qty(_in_orders)`, `supplier`, `reduction_*`, `active`, `categories`, `enriched_at`, `enrich_attempted_at`, `enrich_priority`; nové `catalog_enrich_state`, `shop_revenue_daily`). Plnenie NIE plošným syncom, ale prioritizovaným obohacovaním `src/lib/engine/catalog-enrich.ts` (`runEnrichBatch`, `ENRICH_MAX_PER_RUN=150`, `ENRICH_QUOTA_RESERVE=50`) + `POST /api/catalog/enrich` na dopyt. Testy nad MariaDB. |
| K2 | **splnené v revidovanom tvare (D117/D119)** | `salesRepo.upsertRevenueDay/listRevenue/coverageFor`; medzery sú v DB rozlíšiteľné od núl (`coverage`, `unknownDays`). Tržba len na úrovni eshopu, per produkt kusy. 180-dňový plošný backfill sa vedome nespúšťa (D119 ruší D111). Audit rozpočtu dokázaný integračne: `test/integration/rozpocet-audit.spec.ts` → `spent 2, keyedReadsToday 10, remaining 188`. Migrácia `0016` (stav čítania tržby) je po tomto behu aplikovaná — viď „Nasadenie". |
| K3 | **čiastočne — preklik len nad zlomom** | Endpointy `/api/insights/{revenue-daily,sales-daily,top-products,timeline,product/[productId],product-kpi}` stoja a majú testy; `TopFlopSection` priznáva `rankingState` (`lower_bound` / `unknown`, pri nule dočítaných dní rebríček vôbec nekreslí); `/api/insights/revenue-daily` po nasadení `0016` odpovedá 200 s `dayStates`. Preklik 31. 8. odfotil Prehľad **nad zlomom**; graf s oknami zliav, karta bežiacich zliav a stavový pás pod zlomom overené NEBOLI (screenshot zrolovaného obsahu sa nedal vyrobiť — viď K11). Sekciu ako celok za splnenú nevyhlasujem. Navyše `dayStates`/`emptyDays`/`measuredZeroDays` na obrazovku vôbec neprichádzajú (otvorený bod 13). |
| K4 | **splnené v kóde, testoch aj preklikom** | Stĺpce „PREDANÉ 30 D"/„PREDANÉ 90 D" idú cez pokrytie (`soldUnitsViaCoverage`) a po oprave `kpiUnitsCell()` už nekreslia `≥ 0` na nemeraných riadkoch. Preklik `http://localhost:3070/produkty` po rebuilde: `{"vyskytov_ge0":0,"vyskytov_ge":0,"pomlciek":1003}`. Panel Predajnosti (`[data-testid="filter-sold-unknown"]`) hlási „40 511 produktov má predaj za toto okno NEZNÁMY — do vedier sa nepočítajú a ani jedno ich nevyberie." Testy `produkty-kpi-bunky.spec.ts` (+2), `produkty-v10.spec.ts` (+1), `produkty-detail-filtre.spec.ts`. |
| K5 | **neoveriteľné** | Krivka 90 d s oknami zliav produktu ani uplift neboli v tomto sprinte dokázané — ani testom definície okien, ani preklikom (detail panel je pod zlomom). Splnenie nevyhlasujem. |
| K6 | **splnené v kóde, bez prekliku** | Staršia verzia §7 hlásila „NESPLNENÉ na dvoch z piatich povrchov" (serverové cesty referenciu neposielali). Medzitým to zavreli vlny staviteľov — overené grepom v strome uzatváracieho behu: `src/lib/repo/audit.repo.ts:90` má `CATALOG_COLUMNS = ', c.reference AS reference, c.name AS product_name'` a pole `reference: string | null`, `src/lib/repo/campaign-items.repo.ts:129` to isté; `src/app/api/audit/route.ts`, `src/app/api/audit/[id]/route.ts` a `src/app/api/campaigns/[id]/route.ts` sa na D116/K6 výslovne odvolávajú. Pomlčku skladá obrazovka (`productLabel()`). **Preklikom to overené nebolo** (audit ani detail zľavy nie sú medzi piatimi odfotenými obrazovkami) a zelená brána to netestovala, takže „splnené" tu znamená „v kóde a v testoch staviteľov", nie „videné na obrazovke". |
| K7 | **čiastočne — mutačne neoverené** | Mutačné overenie `no-write-without-confirm` pre presety nebolo v uzatváracom behu vykonané ani hlásené (`preset-nie-je-zapisova-cesta.spec.ts` je nedotknutý a zelený, ale mutáciou overený nebol) — a práve mutáciu §5 pri K7 žiada ako dôkaz, takže splnenie nevyhlasujem. `markUsed()` už volajúceho MÁ: nová route `src/app/api/presets/[presetId]/mark-used/route.ts:86` (overené grepom), teda sľub „naposledy použité zhora" prestal byť prázdny. |
| K8 | **splnené** | Shop sa číta na dopyt (`POST /api/catalog/enrich`), nie pri renderi; obrazovky čítajú lokálnu DB. Fetch guard v `test/setup.ts` povoľuje len loopback a zhodí test aj pri pohltenej výnimke; všetko proti mock shopu (I6). |
| K9 | **splnené** | Viď „Stav brány kvality": 200/200 súborov, 3888/3888 testov, typecheck a lint 0, žiadny nový `.skip`/`.todo`/`.only`, izolácia overená príkazom `ps -W` s filtrom na `vitest` → 0. |
| K10 | **splnené** | `scripts/backup.sh` → `backups/ovl_zliav-20260831-165620.sql.gz` (1,5 MB) PRED nasadením migrácie `0016`. V `backups/` sú 3 gz/sql z posledných dní + 2 historické menované, teda „posledné 3" drží. Migrácie sú aditívne, `0014`/`0015` needitované (`git status` ich neukazuje). |
| K11 | **čiastočne splnené — a práve tento preklik našiel to, čo testy nezachytili** | Preklik prebehol **31. 8. 2026** a screenshoty vznikli pre **päť obrazoviek NAD ZLOMOM**: Prehľad, Produkty, Nová zľava, Zľavy, Nastavenia → Čo smie robiť. **Zrolovaný obsah sa odfotiť nedal** — panel prehliadača po scrolle prestane kompozitovať; overené, že nejde o chybu layoutu (`scrollY 800`, `docH 2005`, 63 sekcií v DOM). Preklik potvrdil ako správne: appka priznáva ban („Shop odmieta našu IP adresu"), referencia je „kód a EAN — zamknuté", nadpis presetov hovorí „PREDPLNIA FORMULÁR NOVEJ ZĽAVY", rozsah hlási „uvoľnenie si vypýta potvrdenie" (D106), rezerva zápisov drží (čítania 53/240, zápisy 200/200). Zároveň **našiel štyri chyby, ktoré 3756 testov nezachytilo** (D121 na serveri, `≥ 0` na 40 511 riadkoch, brána počtu 200 vs. 5, „filtru nevyhovuje ani jeden produkt") — viď poučenie nižšie. |

### Odchýlky od §2 a §2b

1. **Plošné obohacovanie katalógu sa nestavalo** — podľa D118 je prioritizované
   (`enrich_priority` 1/2/3) a na dopyt. Dôvod: 41 348 produktov × `getFull`
   pri ~200/deň = ~207 dní.
2. **€ per produkt nikde nie je** (D117). Tržba len na úrovni eshopu
   (`shop_revenue_daily`), per produkt výhradne kusy; rozdeľovanie `total_paid`
   medzi položky je zakázané.
3. **180-dňový backfill objednávok sa nespustil** (D119 ruší D111). Okná 30/90 d
   ukazujú len naozaj stiahnuté dni a medzeru priznávajú.
4. **Trojstavovosť si vyžiadala výnimku, ktorú §5 nepredvídalo.** Pri čiastočnom
   okne server posiela HODNOTU aj `gap: 'days_missing'` zámerne (doc-blok
   `src/lib/sales/insights.ts`: „hodnota JE, ale je to DOLNÁ HRANICA").
   Uzatvárací beh výnimku ZÚŽIL: `≥ N` sa smie vypísať len keď `value > 0`,
   inak (meraná nula pri chýbajúcich dňoch) je to pomlčka s dôvodom. Dve
   nezávislé závory v `src/components/products/sold-coverage.ts`.
5. **„Plné pokrytie okna" malo dve definície.** Klient porovnával `daysCovered`,
   server `status='complete'`. Zjednotené: `SoldCoverage.daysPartial` je odteraz
   POVINNÉ pole, `parseSoldCoverage()` ho číta strikte (chýba ⇒ `null`, teda
   priznané „nevieme", nie dosadená nula) a nový `fullyReadDays() = daysCovered −
   daysPartial` je tá istá definícia ako serverový `SQL_COMPLETE_DAYS`.
6. **Veľkosť VÝBERU a veľkosť ZÁPISU sú odteraz dve rôzne čísla.** §5 to
   nepredvídalo: `NewDiscount.tsx` má `discountedCount = tieredProductIds.length`
   vedľa `itemsCount` a všade, kde sa hovorí o tom, čo sa zapíše (brána
   potvrdenia, dominanta, odhad dobehnutia, súčet pásiem, vzorka, odtlačok
   dry-runu), sa používa `discountedCount`.
7. **Prepojenie rozpočtov nebolo v §2 vôbec.** `src/lib/engine/budget.ts`
   odteraz odpočítava od zápisového stropu čítania dráhy `product_read`
   (`keyedReadsToday`, dep `keyedReads`) — jeden kľúč, jedna kvóta.
   Dôsledky viď otvorené body 7–9, 14 a 15.

### Čo sprint našiel nad rámec zadania

**Našiel to preklik, nie testy — a to je poučenie tohto sprintu.** Kritérium K11
existuje presne preto, aby sa toto stalo: preklik v prehliadači 31. 8. 2026
odhalil štyri chyby, ktoré **3756 zelených testov nezachytilo**, vrátane
serverovej polovice D121. Príčina je pomenovateľná: route
`GET /api/catalog/search` **nemala ani jeden test** — čo test nevyňal, ale ani
nepokryl, nestráži nikto. Prvá mutácia (`?? 0` na obe miesta) preto
nezčervenala nič; test sa musel dopísať (sekcia 1b v `routes-v8.spec.ts`,
route-level `null/4/0` + tvarová závora nad zdrojom route, tak ako repo grepuje
`setReduction`) a až potom tá istá mutácia dala `2 failed | 9 passed`.

Zavreté nálezy uzatváracieho behu:

- **I11 blokujúci — `≥ 0` na ~40 511 riadkoch.** `kpiUnitsCell()` vykresľovalo
  „aspoň nula" tam, kde appka nevie nič. Zavreté dvomi nezávislými závorami
  (`units.value > 0`; druhá hneď za null-checkom pre prípad, že medzeru ohlási
  len `lowerBound`/`unknownDays`). Tabuľka dáva to isté ako bočný panel.
- **I11 blokujúci — brána potvrdenia lákala na nesprávne číslo.** Potvrdzovala
  200, zapisovalo sa 5. Test to odteraz meria proti POČTU PRODUKTOV V TELE
  ZÁPISU (`discountWriteRequest(...).productIds.length`): výber 5, zápis 2,
  3 neznáme → brána žiada 2, `typed: '5'` neodomkne.
- **I11 blokujúci — `/zlavy/nova` tvrdila „filtru nevyhovuje ani jeden produkt"**
  pri prázdnom výbere a tretí stav zmizol. Zavreté tým, že `SoldCoverageNote` sa
  kreslí aj pri prázdnom výbere, a novou čistou `emptySelectionText()`, ktorá
  rozlišuje „nič nevyhovuje" od „predaje nepoznáme"; `counts.soldUnknown` je
  odteraz na klientovi v OBOCH parseroch bez `?? 0`.
- **Mŕtve tlačidlo „Skúška naprázdno"** — vypnuté je odteraz práve vtedy, keď
  `runPreview()` nemá čo počítať, a `queueBlockedReason()` už nepovie „Vyberte
  aspoň jeden produkt" človeku, ktorý produkty vybral (nové `skippedUnknown`
  povie pravý dôvod).
- **`counts.soldUnknown` nemal konzumenta** — pole je v `CatalogCountsView`
  aj v `CatalogPageView['counts']` a `CatalogFilters.tsx` ho vykresľuje
  (`filter-sold-unknown`); `null` znamená „nevieme, koľko nevieme", `0` mlčí.
- **Mŕtvy klient na neexistujúcu cestu** — `EXTRAS_ENDPOINT =
  '/api/catalog/extras'` a `fetchProductExtras()` (nula volajúcich, route toho
  mena nikdy nevznikla) zmazané; hlavička menuje živú cestu `extras-api.ts` →
  `POST /api/catalog/details`.
- **Dve zastarané hlavičky, ktoré viedli k chybe** — `CatalogTable.tsx` tvrdila,
  že `unitsSold` z `catalog/search` bránu `status='complete'` NEMÁ (po D121 má);
  práve podľa jej starej podoby by niekto vrátil `?? 0`.

Zostáva v platnosti zo staršieho zápisu §7 (nálezy skorších vĺn): mŕtva vetva
`rate_limited` v `executor.ts`, fail-closed `parseTopFlop()`, chýbajúca
`shop_read_budget` v `DATA_TABLES`, rate limit na `reduction-check`, vzorka
v sprievodcovi.

### Otvorené (vedome, mimo rozsahu tohto sprintu)

**Akcie Samuela — bez nich sprint nemá dáta:**

1. **Rotácia kľúča (P0).** Kľúč z 28. 8. je uniknutý (je v transkripte v plnom
   texte). Nový treba vložiť cez Nastavenia → Kľúče. **Je to akcia Samuela,
   agent ju urobiť nemôže.**
2. **Odblokovanie IP** (`docs/60` → delaja@fedorco.sk). Shop vracia
   `{"error":"ip_banned"}` na VŠETKO, aj na verejné čítanie katalógu bez kľúča;
   potvrdené aj v logu bežiacej appky po rebuilde 31. 8. **Kým to platí,
   obohacovanie STOJÍ a KPI z `getFull` sú PRÁZDNE** — appka to povie a dáta
   nemení (D120), ale žiadne číslo z eshopu nepribudne. **Tiež akcia Samuela.**
3. **Preklik pod zlomom (zvyšok K11).** Päť obrazoviek nad zlomom je odfotených;
   zrolovaný obsah sa odfotiť nedá, kým panel prehliadača po scrolle prestane
   kompozitovať. Neoverené preto zostávajú: graf tržby s oknami zliav, karta
   bežiacich zliav, stavový pás, detail panel produktu (teda K3 a K5).

**Rozhodnutia, ktoré agent robiť nesmie:**

4. **`POST /api/queue/resume` dopáli zmeškanú kampaň bez čerstvého dry-runu.**
   `SQL_REQUEUE_MISSED` (`campaigns.repo.ts:216–218`) prehodí `missed` → `queued`,
   hoci `src/app/api/campaigns/[id]/execute/route.ts:5–6` o sebe tvrdí, že je
   „JEDINÁ cesta, ktorou sa zmeškaná kampaň dopáli (D33b)". **Nie je to
   porušenie I3** (dry-run a potvrdenie z vytvorenia existujú, `assertConfirmed()`
   prepočíta `confirm_payload_hash` vrátane `price_at_preview`), ale je to rozpor
   medzi vyhláseným invariantom a kódom plus nepravdivá veta v docbloku. Či má
   „Pokračovať" vynucovať ČERSTVOSŤ potvrdenia, je rozhodnutie o zápisovej ceste
   do produkcie.
5. **`ProductDetailPanel` má tie isté fakty dvakrát** — „Fakty z eshopu"
   (`catalog_cache`, s `measuredNote()`) a „Podrobnosti z eshopu"
   (`extras`/`keyed`, BEZ času merania). Treba rozhodnúť, ktorá skupina zostáva
   a či druhá dostane čas merania.
6. ~~**Presety a `last_used_at`** — `markUsed()`/`update()` bez volajúceho.~~
   **ZAVRETÉ vlnou staviteľov**, nie zelenou bránou: volajúci je route
   `src/app/api/presets/[presetId]/mark-used/route.ts:86` (overené grepom
   `markUsed` v `src/`). Zostáva otvorené len to, že cesta nie je overená
   preklikom ani mutačne (viď K7).

**Nezavreté technické body:**

7. **Cudzia stránka vie odobrať zápisový rozpočet.**
   `GET /api/catalog/reduction-check` neposudzuje `checkOrigin()` (D72 sa na GET
   nevzťahuje) a appka nemá prihlásenie (D98–D100). Denný strop dráhy
   `product_read` (160) sa vyčerpá za ~10 minút a keďže sa čítania odpočítavajú
   od zápisového stropu, zápisový rozpočet klesne z 200 na 40 bez jediného
   POST-u. Pri nečitateľných `settings` platí `FAIL_CLOSED_DAILY_BUDGET = 1` →
   `remaining = 0` a `checkDailyBudget` odmietne CELÚ frontu.
8. **Dva komentáre v `budget.ts` po prepojení rozpočtov tvrdia nepravdu**
   („Zápisy majú prednosť pred obohacovaním" — taká prednosť v kóde neexistuje;
   „pri `dailyWriteBudget = 1` sa fronta nezastaví" — s odpočtom čítaní sa
   zastaví hneď po prvom čítaní).
9. **`POST /api/catalog/details` míňa `getFull` nepočítane** a nemá rate limit —
   jediný veľký spotrebiteľ kvóty kľúča, ktorý do dráhy `product_read` neúčtuje.
10. ~~**Dva endpointy majú NULA konzumentov** — `GET /api/insights/discount-depth`
    (G2) a `GET /api/insights/campaign/[id]/items` (G5).~~ **ZAVRETÉ vlnou
    staviteľov**, nie zelenou bránou: `discount-depth` je z repa zmazaný
    (`git status` → ` D src/app/api/insights/discount-depth/route.ts`, `grep -rn
    discount-depth src/` = 0 zásahov) a `campaign/[id]/items` konzumenta má
    (`src/components/campaigns/DiscountDetail.tsx:59`,
    `zlavy-api.ts:17`). Preklikom detail zľavy overený nebol.
11. **Stav dávky obohacovania nikto nečíta** (D120, polovica):
    `catalog_enrich_state`, `lastEnrichRun()`, `pause_reason='ip_banned'`,
    `paused_until=NULL` nemá žiadnu route ani obrazovku. Dávka môže stáť tri
    týždne a človek to zistí len SELECT-om do DB.
12. ~~**Dokončiť K6** — pripojiť `reference` v `audit.repo.ts` a
    `campaign-items.repo.ts`.~~ **ZAVRETÉ vlnou staviteľov** (viď K6). Otvorené
    zostáva len overenie preklikom: audit ani detail zľavy nie sú medzi piatimi
    odfotenými obrazovkami.
13. **`dayStates`, `emptyDays` a `series[].measuredZeroDays` na obrazovku
    NEPRICHÁDZAJÚ.** `parseRevenueDaily`
    (`src/components/dashboard/window-api.ts:207–215`) číta len `missing.length`
    a `hasGap`; grep tých troch názvov v `src/components` = 0 zásahov. Dôsledok:
    deň dočítaný do konca a bez jedinej objednávky — teda presne to, na čo
    migrácia `0016` vznikla — z grafu tržby zmizne bez bodu aj bez značky.
    **Nezavreté zámerne:** smer je bezpečný (na povrch ide len menší počet
    `missing`, appka nič netvrdí navyše), ale dotiahnutie znamená rozhodnúť, ako
    má v grafe vyzerať stav „prečítané, nepredalo sa nič" — to je dizajnové
    rozhodnutie Samuela, nie vec zelenej brány. Serverová strana žije:
    `curl /api/insights/revenue-daily?days=30` → 200 s `dayStates` (dnes všetko
    `state: "unknown"`).
14. **`product-codes.ts` míňa kľúč `shop_write` mimo rozpočtu.**
    `src/lib/catalog/product-codes.ts:275` robí až `CODE_LOOKUP_MAX = 10`
    volaní `getProductFull()` s kľúčom `shop_write` a nevolá ani
    `reads.reserve()`, ani `reserveShopReads()`; dosiahnuteľné cez
    `GET /api/catalog/search?codes=1&productIds=…`, ktorý nemá `rateLimit` a ako
    GET nejde ani cez origin check. Odvodenie `WRITE_QUOTA_RESERVE = 200 − 160 =
    40` (`src/lib/engine/budget.ts:114`) stojí na tom, že keyed čítania nikdy
    neprelezú dráhu `product_read` — tieto ju neinkrementujú, takže
    `remainingToday()` ohlási plný rozpočet a fronta sa rozbehne do 429 uprostred
    dávky. **Neopravené zámerne:** predchádza tomuto behu (`product-codes.ts` je
    z `68f8ff3`, blok `if (q.codes)` je už v
    `git show 11626fc:src/app/api/catalog/search/route.ts:495`) a oprava sa
    dotýka rozpočtovej dráhy, ktorá chráni zápis do produkčného eshopu — žiada
    si vlastný beh s vlastným mutačným overením.
15. **`keyedReads` sa zapne len keď volajúci nepodsunie `counter`**
    (`src/lib/engine/budget.ts:437–442`). Dnes to v produkcii drží (overené aj
    testom nad produkčným adaptérom), ale je to domnienka o budúcich volajúcich,
    nie závora.
